import React from 'react';
import Card from '../shared/Card';

export default function CalendarWidget() {
  // Placeholder: Replace with real calendar integration or events
  return (
    <Card title="Calendar">
      <div style={{ minHeight: 120 }}>
        <p>Upcoming deadlines and appointments will appear here.</p>
        {/* Integrate with Google Calendar, Outlook, or custom events */}
      </div>
    </Card>
  );
}
